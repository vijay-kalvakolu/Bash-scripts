# Shell, Bash

***Shell***  A Program that interprests and executes the various commands that we type in the terminal and transalte our commands so that the OS Kernal can understand it.

# sh (Bourne Shell)  - /bin/sh
# bash (Bourne Again Shell) - /bin/bash
# csh (C Shell) - /bin/csh
# tcsh (TC Shell) - /bin/tcsh
# ksh (Korn Shell) - /bin/ksh

***shebang***: We tell the OS to use a specided shell or bash with its path
``` #!/bin/bash


 " Bash scripts files are named with extenstion .sh and the file should have permissions to execute the script.
